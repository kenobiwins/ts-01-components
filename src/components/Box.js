import styled from 'styled-components';

import {
  layout,
  color,
  grid,
  background,
  border,
  position,
  display,
  flexDirection,
} from 'styled-system';

export const Box = styled('div')({
  layout,
  display,
  flexDirection,
  color,
  grid,
  background,
  border,
  position,
});
